var names : Array<string>=['jakob','lucas','mila'];

/*for (let index in names){
	console.log(index);
}

for (let index of names){
	console.log(index);
}*/

for (let index in names){
	console.log(index);
	console.log(names[index]);
}