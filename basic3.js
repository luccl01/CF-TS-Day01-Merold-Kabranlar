var names = ['jakob', 'lucas', 'mila'];
/*for (let index in names){
    console.log(index);
}

for (let index of names){
    console.log(index);
}*/
for (var index in names) {
    console.log(index);
    console.log(names[index]);
}
